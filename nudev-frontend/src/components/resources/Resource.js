import styled from "styled-components";
import { PageMain, PRContainer, PRHeader } from "../../styles/SharedStyles";

const Resource = () => {
  return (
    <PageMain>
      <PRContainer>
        <PRHeader>{}</PRHeader>
      </PRContainer>
    </PageMain>
  );
};

export default Resource;
